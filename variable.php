<?php
    /**
     * VARIABLE DECLARATION RULE
     * 1.Variable name start with letter or underscore.
     * 2.Variable names can't start with number.
     * 3.A variable name can ahve any number of letters, Underscore.
     * 4.valid letters are all ASCII hexadecimal from 00 to FF(0-255).
     */
    $a="variable declaration";
    //echo $a;
    /**
     * var_dump() is used for debugging in php
     * it dumps the information about the variable
     * in special case for string it also tells about the length of string
     */
    var_dump($a);
    //   
?>